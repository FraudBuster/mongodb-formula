# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: mongodb
  rootgroup: root
  config: '/etc/mongodb'
  service:
    name: mongodb
  subcomponent:
    config: '/etc/mongodb-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
